# Быстрый старт с 360dialog

## ✅ Что было сделано

1. ✅ Создан клиент `Waba360DialogClient` для работы с 360dialog API
2. ✅ Обновлен `WabaService` для автоматического выбора клиента (360dialog или mock)
3. ✅ Обновлен webhook controller для обработки формата 360dialog
4. ✅ Добавлены переменные окружения в `env.example`
5. ✅ Создана документация `DIALOG360_SETUP.md`

## 🚀 Настройка

### 1. Добавьте переменные в `.env`

```env
DIALOG360_API_KEY=V0Hk6F0fFSfTlEYvhQxjNfzMAK
DIALOG360_BASE_URL=https://waba-v2.360dialog.io
DIALOG360_WEBHOOK_URL=https://your-domain.com/api/webhooks/whatsapp
```

### 2. Настройте Webhook в 360dialog

Выполните команду (замените `your-domain.com` на ваш домен):

```bash
curl -X POST https://waba-v2.360dialog.io/v1/configs/webhook \
  -H "Content-Type: application/json" \
  -H "D360-API-KEY: V0Hk6F0fFSfTlEYvhQxjNfzMAK" \
  -d '{
    "url": "https://your-domain.com/api/webhooks/whatsapp"
  }'
```

**Важно:**
- URL должен быть доступен из интернета (используйте ngrok для локальной разработки)
- URL не должен содержать порт (например, `:3000`)
- URL не должен содержать символы подчеркивания (`_`)

### 3. Перезапустите приложение

```bash
pnpm dev:api
```

## 📝 Проверка работы

1. Отправьте сообщение на ваш WhatsApp номер
2. Проверьте логи приложения - должны появиться сообщения о получении webhook
3. Бот должен автоматически ответить на сообщение

## 🔍 Логи

При успешной инициализации вы увидите в логах:
```
[Waba360DialogClient] 360dialog client initialized
[WabaService] Using 360dialog client
```

## 📚 Дополнительная информация

См. `DIALOG360_SETUP.md` для подробной документации по API.

